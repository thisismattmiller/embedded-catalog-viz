// Where the deployed viewer loads tiles/labels/lookup shards from: the public
// R2 bucket URL (r2.dev development URL or a custom domain), no trailing slash.
// All assets live under the data/ prefix in the bucket.
window.DATA_BASE = "https://lcviz.thisismattmiller.cloud/data";
